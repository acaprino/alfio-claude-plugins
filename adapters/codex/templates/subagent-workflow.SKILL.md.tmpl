---
name: ${workflow}
description: '${description}'
---

# ${workflow}

<!-- Generated by the Daodan compiler for codex. Edit the kernel, never this file. -->

## Execution harness

Selected topology: **${strategy}**, with role delivery by **${role_delivery}**. Start one isolated
runtime subagent per selected role. When role delivery is `inline-prompt`, paste that role's Markdown
body into the dispatch prompt verbatim: the worker must receive the whole role, not a summary of it.

Harness obligations, none of them optional:

- Every worker runs in its own subagent context (`isolation: ${isolation}`), and never reads another
  worker's result.
- Dispatch exactly the selected roles, once each: ${roles}
- Hold the `${join}` barrier: every expected role is recorded `delivered` or `failed` before anything
  downstream runs.
- Only the phase that declares the report artifact may write it.

Role bodies ship with this package under `roles/`, and the phase graph and record schemas under
`contracts/${workflow}.workflow.toml`.

## Method

${body}
