# Provisional until the evidence table in tests/host-probes/README.md is measured.
# Every state and strategy row here must be re-derived from that table.

[capabilities."repository.read"]
state = "native"
strategy = "tool"
value = "read_file"

[capabilities."repository.write"]
state = "native"
strategy = "tool"
value = "apply_patch"

[capabilities."shell.execute"]
state = "native"
strategy = "tool"
value = "shell"

[capabilities."network.fetch"]
state = "adapted"
strategy = "shell-fetch"

[capabilities."contexts.isolate"]
state = "adapted"
strategy = "runtime-subagent"

[capabilities."roles.dispatch"]
state = "adapted"
strategy = "inline-prompt"

[capabilities."execution.parallel"]
state = "adapted"
strategy = "concurrent-dispatch"

[capabilities."tasks.share"]
state = "unsupported"
strategy = "none"

[capabilities."peers.message"]
state = "unsupported"
strategy = "none"

[capabilities."hooks.lifecycle"]
state = "native"
strategy = "hooks-manifest"

# No probe has shown Codex starting an MCP server declared by an installed
# plugin. The package ships the server file, and every workflow of the plugin
# carries a note asking the user to register the command with the host.
[capabilities."mcp.servers"]
state = "adapted"
strategy = "mcp-registration"
