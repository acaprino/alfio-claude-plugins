---
name: ${workflow}-coordinator
description: '${description}'
tools: ['agent', 'search']
agents: [${agents}]
---

# ${workflow}

<!-- Generated by the Daodan compiler for copilot. Edit the kernel, never this file. -->

## Execution harness

Selected topology: **${strategy}**. Dispatch the workers listed in this agent's `agents` allowlist and
nothing else. Request them in parallel; serializing them is allowed and changes no part of the
contract.

Harness obligations, none of them optional:

- Every worker runs in its own context (`isolation: ${isolation}`), and never reads another worker's
  result.
- Dispatch exactly the selected roles, once each: ${roles}
- Hold the `${join}` barrier: every expected worker is recorded `delivered` or `failed` before
  anything downstream runs.
- Only the phase that declares the report artifact may write it.

The phase graph and record schemas ship with this package in
`contracts/${workflow}.workflow.toml`.

## Method

${body}
