# dependency-audit canary fixture

A two-ecosystem project (npm and pip) with deliberately old, pinned versions, used by the
`dependency-audit` canary eval in `evals/universal-daodan/dependency-audit.md`.

Nothing here is ever installed or executed. The versions are old on purpose so that a real audit tool
has something to report, and pinned on purpose so that the eval is repeatable.
