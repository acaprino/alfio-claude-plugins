# Create Documentation

Use the `documentation-engineer` agent to create accurate documentation for:

$ARGUMENTS

## Quick Examples

- `/docs-create src/api` - Document all API endpoints in src/api
- `/docs-create UserService` - Document the UserService class
- `/docs-create` - Document the entire project (requires extended context)
- `/docs-create src/utils --api-only` - Document only public exports
