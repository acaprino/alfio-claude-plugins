schema = "daodan/v1"
name = "csp"
version = "1.4.0"
description = "Constraint Satisfaction Problems and combinatorial optimization with Google OR-Tools CP-SAT solver"
license = "MIT"

[capabilities]
required = ["repository.read", "shell.execute", "repository.write", "network.fetch", "contexts.isolate", "roles.dispatch"]
optional = []

[dependencies]
required = []

[components]
skills = []
roles = ["or-tools-expert"]
workflows = []
policies = []
