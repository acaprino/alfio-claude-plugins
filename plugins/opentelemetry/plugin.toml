schema = "daodan/v1"
name = "opentelemetry"
version = "1.4.0"
description = "OpenTelemetry Python instrumentation - distributed tracing, async context propagation, custom transport propagators, OTLP exporters, AWS ADOT/X-Ray, and production observability; includes /otel-audit command"
license = "MIT"

[capabilities]
required = ["repository.read", "shell.execute", "repository.write", "network.fetch", "contexts.isolate", "roles.dispatch"]
optional = []

[dependencies]
required = []

[components]
skills = ["opentelemetry"]
roles = ["otel-architect"]
workflows = ["otel-audit"]
policies = []
