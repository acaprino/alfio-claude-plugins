schema = "daodan/v1"
name = "app-analyzer"
version = "1.2.0"
description = "Comprehensive app analysis for Android (ADB) and web (Playwright MCP) - navigation mapping, competitive analysis, design system extraction, UX flow analysis, and business model intelligence"
license = "MIT"

[capabilities]
required = ["repository.read", "repository.write", "contexts.isolate", "roles.dispatch"]
optional = []

[dependencies]
required = ["playwright-skill@playwright-skill"]

[components]
skills = []
roles = ["app-analyzer"]
workflows = []
policies = []
