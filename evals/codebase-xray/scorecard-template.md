# Scorecard: <case>

- **Date:**
- **Component run:** `/codebase-xray:analyze` | `/codebase-xray:team-analyze` | `semantic-interconnect-mapper`
- **Host / model / session:**
- **Plugin version under test:**
- **Target materialized:** (scratch path, fixture used, any deliberate edits)

## Assertions

| # | Type | Outcome (pass / fail / n/a) | Evidence |
|---|------|-----------------------------|----------|

Evidence is a quote from the transcript or a file in the run directory, not a summary. An assertion scored `pass` with no evidence is unscored.

## Cost

- Wall-clock:
- Files read by hand (transcript count) versus script invocations:
- Workers dispatched (team mode):

## Observations

[What the pipeline did well or badly. Anything that looked like a near miss: an invariant held for the wrong reason, an assertion that turned out to encode a preference, a case that needs rewriting.]

## Verdict

- MUST assertions: N passed / M total
- Case result: PASS (all MUST passed) | FAIL
